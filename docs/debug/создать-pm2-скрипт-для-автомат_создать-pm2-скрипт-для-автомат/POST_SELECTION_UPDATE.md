# 📊 Post Selection Update - Последние 80 постов

**M7 Session ID:** `task_создать-pm2-скрипт-для-автомат_7683`  
**Дата:** 29 января 2026

---

## 🎯 Ключевое изменение

### ❌ БЫЛО: 20-30 постов
```javascript
take: 20 // или 30
```

### ✅ СТАЛО: 80 постов
```javascript
take: 80 // 🔥 Последние 80 постов!
```

---

## 📊 Логика выборки постов

### Полная функция:

```javascript
async function getRandomPost(excludeUserId) {
  try {
    const posts = await prisma.post.findMany({
      where: {
        creatorId: { not: excludeUserId },
        OR: [
          { isLocked: false },
          { isPremium: false }
        ]
      },
      take: 80, // 🔥 Последние 80 постов!
      orderBy: { createdAt: 'desc' } // Сначала новые
    })
    
    if (posts.length === 0) {
      console.log('[AI Activity Bot] No available posts found')
      return null
    }
    
    // Случайный выбор из последних 80
    return posts[Math.floor(Math.random() * posts.length)]
  } catch (error) {
    console.error('[AI Activity Bot] Error fetching posts:', error)
    return null
  }
}
```

---

## 🔥 Почему 80 постов?

### 1. Актуальность контента
- ✅ **Не берём супер старые посты**
- ✅ Фокус на свежем контенте
- ✅ Релевантная активность

### 2. Достаточное разнообразие
- ✅ 80 постов = большой выбор
- ✅ Меньше повторений
- ✅ Разные темы и типы контента

### 3. Производительность
- ✅ 80 постов = быстрая загрузка
- ✅ Не перегружаем БД
- ✅ Оптимальный баланс

### 4. Естественность
- ✅ Комментарии на актуальных постах
- ✅ Как реальные пользователи
- ✅ Не копаемся в архивах

---

## 📊 Сравнение подходов:

| Параметр | 20 постов | 30 постов | **80 постов** (выбрано) |
|----------|-----------|-----------|--------------------------|
| Актуальность | 🟡 Средняя | 🟡 Средняя | ✅ **Высокая** |
| Разнообразие | 🔴 Низкое | 🟡 Среднее | ✅ **Высокое** |
| Повторения | 🔴 Часто | 🟡 Иногда | ✅ **Редко** |
| Старые посты | ❌ Могут попасть | ❌ Могут попасть | ✅ **Исключены** |
| Производительность | ✅ Отлично | ✅ Отлично | ✅ **Отлично** |

---

## 🎯 Что гарантируется:

### ✅ Актуальность:
- Берём **последние 80 постов** (orderBy: createdAt DESC)
- **НЕ берём** супер старые посты
- Только свежий контент

### ✅ Разнообразие:
- 80 вариантов для выбора
- Меньше шансов попасть на один и тот же пост
- Разные категории и типы

### ✅ Исключения:
- ❌ Свои посты (not excludeUserId)
- ❌ Заблокированные (isLocked: true)
- ❌ Премиум без доступа (isPremium: true)
- ❌ Супер старые (за пределами топ-80)

---

## 📊 Вероятность повторений:

### При 20 постах:
- Вероятность одного поста: **5%** (1/20)
- Повторение за день: **~5 раз** (96 проходов)

### При 30 постах:
- Вероятность одного поста: **3.3%** (1/30)
- Повторение за день: **~3 раза**

### При 80 постах (выбрано):
- Вероятность одного поста: **1.25%** (1/80)
- Повторение за день: **~1 раз**
- ✅ **Минимум повторений!**

---

## 🔄 Workflow с 80 постами:

```
START
  ↓
Выбран AI пользователь (ID: user123)
  ↓
Query: SELECT * FROM posts 
  WHERE creatorId != 'user123'
  AND (isLocked = false OR isPremium = false)
  ORDER BY createdAt DESC
  LIMIT 80  ← 🔥 Последние 80!
  ↓
Получено: 80 актуальных постов
  ↓
Random pick: posts[Math.floor(Math.random() * 80)]
  ↓
Выбран: пост #42 из последних 80
  ↓
✅ Пост актуальный, не супер старый!
```

---

## 💡 Примеры сценариев:

### Сценарий 1: Активный проект
```
Всего постов в БД: 500
Последние 80: посты за последние 5-7 дней
Супер старые: посты за 2-3 месяца назад
✅ Берём только свежие!
```

### Сценарий 2: Новый проект
```
Всего постов в БД: 50
Последние 80: все 50 постов
Супер старые: нет
✅ Берём все доступные!
```

### Сценарий 3: Зрелый проект
```
Всего постов в БД: 5000
Последние 80: посты за последние 1-2 дня
Супер старые: посты за несколько месяцев
✅ Максимальная актуальность!
```

---

## 🎯 Итого:

**80 постов = оптимальный баланс!**

✅ **Актуальность** - не берём супер старые  
✅ **Разнообразие** - большой выбор  
✅ **Производительность** - быстро  
✅ **Естественность** - как реальные пользователи  
✅ **Минимум повторений** - 1.25% шанс на один пост

**Обновлено в документации:**
- SOLUTION_PLAN.md - Phase 3
- DISCOVERY_REPORT.md - Post Selection + код функции
